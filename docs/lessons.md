# Lessons (code-side mirror)

Canonical source: Notion **Lessons DB**.

Reflector writes one row per Applier run:

| id | signature (inputs_hash) | outcome | delta_predicted | delta_actual | generalization | tag |
| -- | ----------------------- | ------- | --------------- | ------------ | -------------- | --- |
| L-001 | _first real lesson goes here_ | | | | | |

Tags: `keep`, `do-not-repeat`, `needs-operator`, `needs-operator-review`.
