# Proposals (code-side mirror)

Canonical source: Notion **Proposals DB** (to be created under HomeBase Automation Center).

Every row is one Proposal record. Canonical fields are defined in `src/alpha/types.ts` and ALPHA.md.
